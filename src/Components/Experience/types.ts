export interface ExperiencedType {
	title: string
	data: string[]
	company: string
	isPresent?: boolean
	from: string // change to Data
	to?: string // change to Data
	location?: string
	description?: string
}